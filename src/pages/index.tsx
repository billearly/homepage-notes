import React from "react";
import { NoteGrid } from "../components";
import { MainLayout } from "../layouts";

export default () => {
  return (
    <MainLayout>
      <NoteGrid />
    </MainLayout>
  );
};
