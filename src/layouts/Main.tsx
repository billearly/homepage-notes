import React from "react";
import "../components/assets/global.scss";

// Mainly a placeholder right now
// Include helmet and global style here
// Maybe header and footer
export const MainLayout = ({ children }) =>
  <>
    {children}
  </>
