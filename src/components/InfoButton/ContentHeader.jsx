import styled from "styled-components";

export const ContentHeader = styled.h2`
  font-family: 'Satisfy', cursive;
  font-weight: normal;
  margin: 0;
  padding-bottom: ${props => props.theme.paddingX2};
`;
